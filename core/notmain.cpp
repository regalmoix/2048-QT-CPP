#include "core/global.h"
//#include <curses.h>
using namespace std;
/*
int score = 0;

std::vector<std::pair<int, int>> emptypos;


int main()
{
    int n = 4;

    vector<vector<int>> board(n, vector<int> (n,0));

    boardinit(n);
    
    input(board, n);

    printboard(board, n); 

    //return endwin();
    return 0;

}

*/